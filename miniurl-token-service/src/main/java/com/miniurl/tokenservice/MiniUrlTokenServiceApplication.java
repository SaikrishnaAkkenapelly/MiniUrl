package com.miniurl.tokenservice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** /** Starting point of control for the service. */
@SpringBootApplication
public class MiniUrlTokenServiceApplication {

  public static void main(String[] args) {
    SpringApplication.run(MiniUrlTokenServiceApplication.class, args);
  }
}
